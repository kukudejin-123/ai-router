# Prometheus 配置文件
# 用于抓取 AI-Router 的监控指标

global:
  scrape_interval: 15s # 抓取间隔
  evaluation_interval: 15s # 评估间隔
  external_labels:
    cluster: 'ai-router'
    env: 'production'

# 抓取配置
scrape_configs:
  # AI-Router 应用监控
  - job_name: 'ai-router'
    metrics_path: '/api/actuator/prometheus'
    static_configs:
      - targets: ['localhost:8123']
        labels:
          application: 'ai-router'
          instance: 'ai-router-1'